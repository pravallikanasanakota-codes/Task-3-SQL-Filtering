
##  Objective
The goal of this task is to practice and demonstrate advanced SQL data filtering techniques using the `WHERE` clause along with logical operators and pattern matching.

---

##  Tools Used
- MySQL Workbench  
- SQL (MySQL compatible)  
- Notepad (for writing `.sql` file)  
- GitHub (for submission)

---

##  Files Included
- `advanced_filtering.sql` – SQL queries demonstrating filtering concepts  
- `README.md` – Explanation of the task and queries  
- `screenshots/` (optional) – Query execution results from MySQL Workbench  

---

##  Concepts Covered
- Filtering data using `WHERE`
- Combining conditions with `AND` and `OR`
- Pattern matching using `LIKE`
- Wildcards `%` and `_`
- Filtering using `IN`
- Range filtering using `BETWEEN`
- Handling NULL values using `IS NULL`
- Using table aliases for readability
- Real-life examples (email filtering)

---

##  Sample Scenarios Implemented
- Fetch students above a certain age  
- Filter students from specific cities  
- Search names starting with a specific letter  
- Find emails from a specific domain  
- Handle missing (NULL) email records  

---

##  Interview Questions Covered
- Difference between `=` and `LIKE`
- Purpose of `%` in `LIKE`
- How SQL handles `NULL`
- When to use `BETWEEN`
- Difference between `AND` and `OR`

---

##  Outcome
After completing this task, I am able to:
- Fetch precise records based on multiple conditions
- Write clean and readable SQL queries
- Apply filtering techniques used in real-world applications

---

## 📤 Submission
This repository is created as part of an internship task su
